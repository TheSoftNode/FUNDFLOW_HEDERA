import BrowseCampaigns from '@/components/campaigns/BrowseCampaigns'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
   <BrowseCampaigns />
  )
}

export default page